## Task Completion Requirements

- All of pnpm format, pnpm lint, and pnpm typecheck must pass before considering tasks completed.

Don't build the project, just assume that `link-watch` is always running
